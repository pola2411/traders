    <a href="" data-direccionip="{{ $direccion_ip }}" data-fechaentrada="{{ $fecha_entrada }}"
        data-fechasalida="{{ $fecha_salida }}" data-tipodispositivo="{{ $dispositivo }}"
        data-so="{{ $sistema_operativo }}" data-navegador="{{ $navegador }}" data-nombre="{{ $user_nombre }}"
        data-correo="{{ $user_correo }}" data-foto="{{ $user_fotoperfil }}" data-tipoaccion="{{ $tipo_accion }}"
        data-tabla="{{ $tabla }}" type="button" title="Vista previa"
        class="btn btn-primary btn-sm btn-icon view"> <i class="bi bi-eye"></i></a>
    <a href="" data-id="{{ $logsid }}" type="button" title="Eliminar modelo"
        class="btn btn-danger btn-sm btn-icon delete"> <i class="bi bi-trash"></i></a>
